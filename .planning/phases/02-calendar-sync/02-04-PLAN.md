---
phase: 02-calendar-sync
plan: 04
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - app/(tabs)/settings.tsx
  - src/components/calendar/CalendarSettingsSection.tsx
  - src/components/calendar/index.ts
autonomous: false
requirements:
  - CAL-04
  - CAL-05
  - CAL-06

must_haves:
  truths:
    - "User can see connected calendar providers in Settings with status and green dot (D-04)"
    - "User can manage calendar toggles from Settings (D-03, D-06)"
    - "User can tag/untag a Work Schedule calendar from Settings (D-07)"
    - "User can toggle native calendar writing on/off (D-11)"
    - "User can change the target write calendar (D-12)"
    - "User can disconnect a provider from Settings"
    - "User can change notification preference for calendar changes (D-15)"
  artifacts:
    - path: "src/components/calendar/CalendarSettingsSection.tsx"
      provides: "Settings section for calendar management"
      exports: ["CalendarSettingsSection"]
    - path: "app/(tabs)/settings.tsx"
      provides: "Settings screen with calendar section added"
  key_links:
    - from: "src/components/calendar/CalendarSettingsSection.tsx"
      to: "src/lib/calendar/calendar-store.ts"
      via: "useCalendarStore for all state reads and writes"
      pattern: "useCalendarStore"
    - from: "app/(tabs)/settings.tsx"
      to: "src/components/calendar/CalendarSettingsSection.tsx"
      via: "import and render in settings scroll"
      pattern: "CalendarSettingsSection"
---

<objective>
Add a full Calendar Management section to the Settings screen: connected provider cards, calendar toggles, Work Schedule tag, write preferences, notification preferences, and disconnect options. Also accessible for users who skipped calendar setup during onboarding (D-01).

Purpose: Implements the ongoing management UX for calendar sync — users can adjust their setup, change preferences, and connect/disconnect providers at any time.
Output: CalendarSettingsSection component integrated into the existing Settings screen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-calendar-sync/02-CONTEXT.md
@.planning/phases/02-calendar-sync/02-01-SUMMARY.md
@.planning/phases/02-calendar-sync/02-02-SUMMARY.md
@.planning/phases/02-calendar-sync/02-03-SUMMARY.md

<interfaces>
From src/lib/calendar/calendar-store.ts:
```typescript
useCalendarStore: {
  appleConnected: boolean;
  googleConnected: boolean;
  appleCalendars: CalendarMeta[];
  googleCalendars: CalendarMeta[];
  workCalendarId: string | null;
  writeToNativeCalendar: boolean;
  nativeWriteCalendarId: string | null;
  changeNotificationMode: ChangeNotificationMode;
  lastSyncedAt: string | null;
  connectApple: (...) => void;
  disconnectApple: () => void;
  connectGoogle: (...) => void;
  disconnectGoogle: () => void;
  toggleCalendar: (id: string) => void;
  setWorkCalendarId: (id: string | null) => void;
  setWriteToNativeCalendar: (enabled: boolean) => void;
  setNativeWriteCalendarId: (id: string | null) => void;
  setChangeNotificationMode: (mode: ChangeNotificationMode) => void;
}
```

From src/components/calendar/index.ts:
```typescript
export { CalendarProviderCard } from './CalendarProviderCard';
export { CalendarToggleList } from './CalendarToggleList';
```

Settings screen pattern (from app/(tabs)/settings.tsx top):
- Uses ScrollView with sections
- Section headers with heading3 style
- Switch rows, pressable rows
- Theme tokens: BACKGROUND, TEXT, ACCENT, BORDER, SPACING, RADIUS
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CalendarSettingsSection component</name>
  <files>
    src/components/calendar/CalendarSettingsSection.tsx,
    src/components/calendar/index.ts
  </files>
  <read_first>
    app/(tabs)/settings.tsx,
    src/components/calendar/CalendarProviderCard.tsx,
    src/components/calendar/CalendarToggleList.tsx,
    src/lib/calendar/calendar-store.ts,
    src/lib/calendar/calendar-service.ts,
    src/lib/calendar/google-calendar-api.ts,
    src/theme/colors.ts
  </read_first>
  <action>
    Create `src/components/calendar/CalendarSettingsSection.tsx`.

    This is a self-contained section component that reads from useCalendarStore and renders all calendar management UI. Structure:

    **Section Header:** "Calendar Sync" with heading3 style, TEXT.primary. Right-aligned: "Last synced {relative time}" in caption style, TEXT.secondary (from `lastSyncedAt`, use `formatDistanceToNow` from date-fns).

    **Provider Cards** (D-04):
    - Render CalendarProviderCard for Apple (always shown). If not connected, onConnect triggers the same flow as onboarding (requestCalendarAccess → fetchAppleCalendars → getOrCreateShiftWellCalendar → connectApple). If connected, show "Connected — {N} calendars" with green dot. onManage expands the calendar toggle section.
    - Render CalendarProviderCard for Google (always shown). Same connect/manage pattern. Google connect uses GoogleSignin.signIn → getTokens → fetchGoogleCalendarList → connectGoogle.

    **Calendar Toggles** (shown when user taps "Manage" on a connected provider, or always visible if at least one provider connected):
    - Render CalendarToggleList with combined apple + google calendars from store.
    - Include Work Schedule tag option (D-07).

    **Write Preferences** section (only visible if at least one provider connected):
    - "Write sleep blocks to native calendar" — Switch toggle. Maps to `writeToNativeCalendar`. Label: "Also add to your default calendar" (body, TEXT.primary). Subtext: "Sleep windows and naps appear in your regular calendar app" (bodySmall, TEXT.secondary). (D-11)
    - "Target calendar" — shown only if writeToNativeCalendar is true. A simple Pressable row that shows current target calendar name (from store, resolved against calendar list). Tapping opens a modal/action sheet with writable calendars to pick from. (D-12)

    **Change Notifications** section (D-15):
    - "When shifts change" — three radio-style options:
      - "Silent" — just update in background
      - "Badge" — show app badge
      - "Push notification" — send push
    - Current selection highlighted with ACCENT.primary. Render as small pressable pills/chips in a row.

    **Disconnect** section:
    - If Apple connected: "Disconnect Apple Calendar" pressable text in SEMANTIC.error color. Shows Alert.alert confirmation before calling `disconnectApple()` and `unregisterCalendarBackgroundSync()` (if no providers left).
    - If Google connected: "Disconnect Google Calendar" same pattern with `disconnectGoogle()` and `GoogleSignin.signOut()`.

    Update `src/components/calendar/index.ts` to add: `export { CalendarSettingsSection } from './CalendarSettingsSection';`
  </action>
  <verify>
    <automated>npx jest --passWithNoTests && grep "CalendarSettingsSection" src/components/calendar/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - CalendarSettingsSection.tsx contains "Calendar Sync" (section header)
    - CalendarSettingsSection.tsx contains "CalendarProviderCard" (renders provider cards, D-04)
    - CalendarSettingsSection.tsx contains "CalendarToggleList" (renders toggles, D-03)
    - CalendarSettingsSection.tsx contains "writeToNativeCalendar" (D-11)
    - CalendarSettingsSection.tsx contains "nativeWriteCalendarId" (D-12)
    - CalendarSettingsSection.tsx contains "changeNotificationMode" (D-15)
    - CalendarSettingsSection.tsx contains "Silent" and "Badge" and "Push" (notification options)
    - CalendarSettingsSection.tsx contains "Disconnect" (disconnect option)
    - CalendarSettingsSection.tsx contains "useCalendarStore"
    - index.ts contains "CalendarSettingsSection"
  </acceptance_criteria>
  <done>
    CalendarSettingsSection component provides full calendar management: provider cards with status, calendar toggles, work schedule tag, write preferences (D-11, D-12), notification preferences (D-15), and disconnect options. Exported from barrel file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate CalendarSettingsSection into Settings screen</name>
  <files>
    app/(tabs)/settings.tsx
  </files>
  <read_first>
    app/(tabs)/settings.tsx,
    src/components/calendar/CalendarSettingsSection.tsx
  </read_first>
  <action>
    Edit `app/(tabs)/settings.tsx`:

    1. Add import: `import { CalendarSettingsSection } from '@/src/components/calendar';`

    2. Find the appropriate location in the ScrollView to insert the section. Place it AFTER the existing sync/export section (look for the Supabase sync or export-related section) and BEFORE account/auth sections. The exact insertion point: find the section that handles data export (ICS export) — calendar settings logically follows data management.

    3. Insert: `<CalendarSettingsSection />` as a standalone section in the scroll.

    4. Ensure no duplicate imports — the file already imports from theme, stores, etc. Only add what's new.
  </action>
  <verify>
    <automated>npx jest --passWithNoTests && grep "CalendarSettingsSection" app/\(tabs\)/settings.tsx</automated>
  </verify>
  <acceptance_criteria>
    - settings.tsx contains "import { CalendarSettingsSection } from '@/src/components/calendar'"
    - settings.tsx contains "<CalendarSettingsSection" (component rendered)
    - settings.tsx has no duplicate imports
    - All existing tests still pass
  </acceptance_criteria>
  <done>
    CalendarSettingsSection is rendered in the Settings screen. Users can manage calendar connections, toggles, write preferences, and notification settings from Settings at any time (D-01 — accessible from Settings anytime).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of calendar Settings management</name>
  <action>
    Full calendar management in Settings: provider cards with connection status, calendar toggles, Work Schedule tag, write preferences, notification preferences, disconnect options. Also the complete end-to-end calendar sync flow from onboarding through to Settings management.
  </action>
  <how-to-verify>
    1. Run `npx expo start` and open the app
    2. Navigate to Settings tab
    3. Verify "Calendar Sync" section is visible
    4. If no calendars connected: verify both provider cards show "Connect" button
    5. Connect Apple Calendar → verify calendar list appears with toggles
    6. Toggle a calendar off → verify it updates
    7. Tag a calendar as Work Schedule → verify "Work" badge appears
    8. Check "Write sleep blocks to native calendar" toggle
    9. Check notification preference pills (Silent / Badge / Push)
    10. Verify "Disconnect" option appears with confirmation dialog
    11. Overall: dark theme, gold accents, no visual glitches, premium feel
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
```bash
# All tests pass
npx jest --passWithNoTests

# Settings integration
grep "CalendarSettingsSection" app/\(tabs\)/settings.tsx

# Component exports
grep -c "export" src/components/calendar/index.ts
```
</verification>

<success_criteria>
- CalendarSettingsSection renders in Settings with provider cards, toggles, write preferences, and notification settings
- Users who skipped onboarding calendar step can connect from Settings (D-01)
- Connected providers show green status dot and calendar count (D-04)
- All write preferences (D-11, D-12) and notification preferences (D-15) are configurable
- Disconnect works with confirmation
- Human verification confirms Settings UX works
</success_criteria>

<output>
After completion, create `.planning/phases/02-calendar-sync/02-04-SUMMARY.md`
</output>
