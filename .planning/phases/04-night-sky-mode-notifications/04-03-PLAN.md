---
phase: 04-night-sky-mode-notifications
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/components/night-sky/StarParticles.tsx
  - src/components/night-sky/RechargeArc.tsx
  - src/components/night-sky/BedtimeTipCycler.tsx
  - src/components/night-sky/NightSkyOverlay.tsx
  - src/components/night-sky/index.ts
autonomous: true
requirements:
  - NSM-02
  - NSM-03
  - NSM-04
  - NSM-05

must_haves:
  truths:
    - "NightSkyOverlay renders a full-screen dark gradient background with star particle animations"
    - "RechargeArc renders a circular gold arc that fills proportionally to fillFraction (0.0 to 1.0)"
    - "RechargeArc fills decrease in real-time if fillFraction prop decreases — animated with withTiming 1500ms"
    - "BedtimeTipCycler rotates through water/phone/temperature tips on a 6-second interval"
    - "NightSkyOverlay shows alarm time, latest wake time, and tomorrow's schedule summary"
    - "StarParticles generates 30 particles with randomized positions using useMemo (not on each render)"
  artifacts:
    - path: "src/components/night-sky/NightSkyOverlay.tsx"
      provides: "Full-screen modal overlay shell combining all sub-components"
      exports: ["NightSkyOverlay"]
    - path: "src/components/night-sky/StarParticles.tsx"
      provides: "Reanimated 4 particle field — 30 dots with fade/drift animations"
      exports: ["StarParticles"]
    - path: "src/components/night-sky/RechargeArc.tsx"
      provides: "SVG circular arc animation using react-native-svg + Reanimated 4"
      exports: ["RechargeArc"]
    - path: "src/components/night-sky/BedtimeTipCycler.tsx"
      provides: "Auto-cycling bedtime tip display (water, phone, temperature)"
      exports: ["BedtimeTipCycler"]
    - path: "src/components/night-sky/index.ts"
      provides: "Barrel export for night-sky components"
  key_links:
    - from: "src/components/night-sky/RechargeArc.tsx"
      to: "react-native-svg"
      via: "Animated.createAnimatedComponent(Circle)"
      pattern: "createAnimatedComponent.*Circle"
    - from: "src/components/night-sky/NightSkyOverlay.tsx"
      to: "src/components/night-sky/StarParticles.tsx"
      via: "import StarParticles"
      pattern: "StarParticles"
    - from: "src/components/night-sky/NightSkyOverlay.tsx"
      to: "src/components/night-sky/RechargeArc.tsx"
      via: "import RechargeArc"
      pattern: "RechargeArc"
---

<objective>
Build the Night Sky Mode visual components: StarParticles background, RechargeArc hero animation, BedtimeTipCycler, and the NightSkyOverlay shell that composes them with the three required info panels.

Purpose: Night Sky Mode must feel like looking at a calm night sky (D-02). The recharge animation is the hero element — it must feel alive, not like a progress bar (D-04). These components are UI-only; wind-down window detection and Today screen wiring come in Plan 04.

Output: Five files in src/components/night-sky/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-night-sky-mode-notifications/04-01-SUMMARY.md

<interfaces>
<!-- react-native-svg + Reanimated 4 arc pattern (from research, verified): -->
import Animated, { useSharedValue, useAnimatedProps, withTiming, Easing, useEffect } from 'react-native-reanimated';
import Svg, { Circle } from 'react-native-svg';
const AnimatedCircle = Animated.createAnimatedComponent(Circle);
// RADIUS = 80, CIRCUMFERENCE = 2 * Math.PI * RADIUS
// strokeDashoffset = CIRCUMFERENCE * (1 - fillFraction)

<!-- Reanimated 4 particle pattern: -->
import { useSharedValue, withRepeat, withSequence, withTiming, withDelay, cancelAnimation, useAnimatedStyle } from 'react-native-reanimated';

<!-- Design tokens: -->
BACKGROUND.primary = '#0A0E1A'    // deep navy — Night Sky bg base
BACKGROUND.surface = '#141929'    // cards
BACKGROUND.elevated = '#1C2137'   // arc track color
ACCENT.primary = '#C8A84B'        // gold — arc fill color
TEXT.primary = '#FFFFFF'
TEXT.secondary = '#9CA3AF'

<!-- PlanBlock type from plan-store: -->
interface PlanBlock { type: SleepBlockType; start: Date; end: Date; label: string; priority: number; }

<!-- NightSkyOverlay props shape to implement: -->
interface NightSkyOverlayProps {
  alarmTime: Date;            // main-sleep block start
  latestWakeTime: Date;       // main-sleep block end
  tomorrowSchedule: string[]; // array of label strings for blocks starting tomorrow morning
  fillFraction: number;       // 0.0-1.0 for recharge arc
  onDismiss: () => void;      // manual close handler
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StarParticles and RechargeArc components</name>
  <files>
    src/components/night-sky/StarParticles.tsx
    src/components/night-sky/RechargeArc.tsx
  </files>
  <action>
First run: `ls src/components/night-sky/ 2>/dev/null` to confirm directory doesn't exist yet. Create it.

**StarParticles.tsx** — 30 animated dots with Reanimated 4 withRepeat loops:
- Generate 30 particles at mount via `useMemo(() => Array.from({ length: 30 }, (_, i) => ({ id: i, x: pseudoRandom(i, 0) * width, y: pseudoRandom(i, 1) * height, size: 1 + pseudoRandom(i, 2) * 2, delay: pseudoRandom(i, 3) * 3000 })), [])` where pseudoRandom uses deterministic seeded values (e.g., `Math.abs(Math.sin(seed * 9301 + 49297)) % 1`) — no Math.random() in useMemo (not truly random but avoids re-render issues)
- Each StarParticle sub-component: `opacity = useSharedValue(0)`, `translateY = useSharedValue(0)`. In useEffect: start withDelay(delay, withRepeat(withSequence(withTiming(0.8, {duration:2000}), withTiming(0.1, {duration:2000})), -1, true)) for opacity and withRepeat(withTiming(-8, {duration:4000}), -1, true) for translateY. Return cleanup: `cancelAnimation(opacity); cancelAnimation(translateY)`.
- Particle view: `position:'absolute'`, white, borderRadius size/2, use useAnimatedStyle
- Use `import { useWindowDimensions } from 'react-native'` for width/height

**RechargeArc.tsx** — SVG circular arc with Reanimated 4:
```typescript
const RADIUS = 80;
const CIRCUMFERENCE = 2 * Math.PI * RADIUS;

// Props: { fillFraction: number } — 0.0 (empty) to 1.0 (full)
// offset = CIRCUMFERENCE * (1 - fillFraction)
```
- `const offset = useSharedValue(CIRCUMFERENCE * (1 - fillFraction))`
- Use Reanimated's `useEffect` (from react-native-reanimated, runs in worklet context) to animate: `offset.value = withTiming(CIRCUMFERENCE * (1 - fillFraction), { duration: 1500, easing: Easing.out(Easing.cubic) })`
- `AnimatedCircle = Animated.createAnimatedComponent(Circle)` at MODULE scope (not inside component)
- Background circle: stroke '#1C2137' (BACKGROUND.elevated), strokeWidth 12, no fill
- Animated circle: stroke '#C8A84B' (ACCENT.primary), strokeWidth 12, no fill, strokeDasharray CIRCUMFERENCE, animatedProps for strokeDashoffset, strokeLinecap 'round', transform 'rotate(-90 90 90)' to start from top
- SVG viewBox '0 0 180 180', width 180, height 180
- Center label: large white text showing Math.round(fillFraction * 100) + '%', positioned absolutely centered
  </action>
  <verify>
    <automated>cd /Users/claud/Projects/ShiftWell && grep -n "createAnimatedComponent" src/components/night-sky/RechargeArc.tsx && grep -n "withRepeat" src/components/night-sky/StarParticles.tsx</automated>
  </verify>
  <done>Both files exist. grep confirms createAnimatedComponent in RechargeArc and withRepeat in StarParticles. npx tsc --noEmit returns no new errors in these files.</done>
</task>

<task type="auto">
  <name>Task 2: BedtimeTipCycler and NightSkyOverlay shell</name>
  <files>
    src/components/night-sky/BedtimeTipCycler.tsx
    src/components/night-sky/NightSkyOverlay.tsx
    src/components/night-sky/index.ts
  </files>
  <action>
**BedtimeTipCycler.tsx** — cycles 3 tips on 6-second interval per D-05:
```typescript
const TIPS = [
  { emoji: '💧', text: 'Drink a glass of water now — it helps regulate body temperature overnight.' },
  { emoji: '📱', text: 'Place your phone face-down across the room — out of reach, out of mind.' },
  { emoji: '🌡️', text: 'Ideal sleep temp is 65-68°F (18-20°C). Lower your thermostat if you can.' },
];
```
- `const [index, setIndex] = useState(0)` cycling with `setInterval(() => setIndex(i => (i + 1) % TIPS.length), 6000)` cleaned up on unmount
- Render: emoji large (28px), tip text in TEXT.secondary style, subtle animated fade using Reanimated withTiming on opacity when index changes (optional — can use simple state transition)
- Component is subtle, not intrusive — small text below the RechargeArc

**NightSkyOverlay.tsx** — full-screen modal overlay composing all components:

Props:
```typescript
interface NightSkyOverlayProps {
  alarmTime: Date;
  latestWakeTime: Date;
  tomorrowSchedule: string[];  // labels of first 3 tomorrow blocks
  fillFraction: number;
  onDismiss?: () => void;
}
```

Layout (top to bottom, `position:'absolute', top:0, left:0, right:0, bottom:0`):
1. Background: `View` with `backgroundColor: '#0A0E1A'` filling entire screen, `zIndex: 100`
2. `<StarParticles />` as absolute background layer inside the overlay
3. `<SafeAreaView>` wrapping content (import from react-native-safe-area-context)
4. ScrollView or View with content:
   - Small dismiss button top-right: `onPress={onDismiss}` with '×' or 'Done' label in TEXT.secondary — per D-02 minimal UI
   - `<RechargeArc fillFraction={fillFraction} />` centered, with label above: "Sleep Quality" in TEXT.secondary
   - Alarm confirmation section: "⏰ Alarm set for {format(alarmTime, 'h:mm a')}" in large white text
   - Latest wake section: "Latest wake: {format(latestWakeTime, 'h:mm a')}" in TEXT.secondary
   - Tomorrow morning section: "Tomorrow" header + list of tomorrowSchedule items in TEXT.secondary (max 3)
   - `<BedtimeTipCycler />` at bottom

Use only COLORS/BACKGROUND/TEXT/ACCENT tokens from `@/src/theme`. Never hardcode color values except where matching exactly to the dark sky palette ('#0A0E1A' is BACKGROUND.primary — use the token).

Use `format` from date-fns for time display.

**index.ts** barrel:
```typescript
export { NightSkyOverlay } from './NightSkyOverlay';
export { StarParticles } from './StarParticles';
export { RechargeArc } from './RechargeArc';
export { BedtimeTipCycler } from './BedtimeTipCycler';
```
  </action>
  <verify>
    <automated>cd /Users/claud/Projects/ShiftWell && npx tsc --noEmit 2>&1 | grep -E "night-sky|NightSky|StarParticle|RechargeArc|BedtimeTip" | head -20</automated>
  </verify>
  <done>npx tsc --noEmit shows no TypeScript errors in src/components/night-sky/ files. All four component files exist. index.ts exports all four.</done>
</task>

</tasks>

<verification>
- `ls src/components/night-sky/` shows 5 files
- `grep -n "NightSkyOverlayProps" src/components/night-sky/NightSkyOverlay.tsx` returns a match
- `grep -n "fillFraction" src/components/night-sky/RechargeArc.tsx` returns a match
- `grep -n "TIPS" src/components/night-sky/BedtimeTipCycler.tsx` returns a match with 3 tip entries
- `npx tsc --noEmit 2>&1 | grep night-sky` — empty (no TS errors)
</verification>

<success_criteria>
- All five night-sky component files created (NSM-02, NSM-03, NSM-04, NSM-05 implemented as components)
- NightSkyOverlay renders three required info items: alarm time, latest wake time, tomorrow schedule
- RechargeArc uses react-native-svg + Reanimated 4 with createAnimatedComponent at module scope
- StarParticles uses withRepeat loops with cancelAnimation cleanup
- BedtimeTipCycler cycles through 3 tips on 6s interval
- No TypeScript errors in new files
</success_criteria>

<output>
After completion, create `.planning/phases/04-night-sky-mode-notifications/04-03-SUMMARY.md`
</output>
