---
phase: 04-night-sky-mode-notifications
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/notifications/notification-service.ts
  - src/lib/notifications/index.ts
  - app/_layout.tsx
autonomous: true
requirements:
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04
  - NOTIF-05

must_haves:
  truths:
    - "Wind-down notification copy uses warm emoji tone: '🌙 Wind-down time — your sleep window opens in X minutes'"
    - "Caffeine cutoff notification copy uses: '☕ Last call for caffeine — cutoff in 30 minutes'"
    - "Morning brief notification is scheduled at wake block time with score placeholder and first block label"
    - "schedulePlanNotifications reads windDownEnabled/caffeineCutoffEnabled/morningBriefEnabled from notification-store before scheduling"
    - "Foreground notifications display in SDK 55 (shouldShowBanner: true, not shouldShowAlert)"
  artifacts:
    - path: "src/lib/notifications/notification-service.ts"
      provides: "Upgraded notification service with warm copy, morning brief, preference offsets"
      exports: ["scheduleMorningBrief", "schedulePlanNotifications", "scheduleWindDownReminder"]
    - path: "app/_layout.tsx"
      provides: "setNotificationHandler registered at app root with SDK 55 API"
      contains: "shouldShowBanner: true"
  key_links:
    - from: "src/lib/notifications/notification-service.ts"
      to: "src/store/notification-store"
      via: "useNotificationStore.getState()"
      pattern: "useNotificationStore\\.getState"
    - from: "app/_layout.tsx"
      to: "expo-notifications"
      via: "Notifications.setNotificationHandler"
      pattern: "setNotificationHandler"
---

<objective>
Upgrade the existing notification service with warm emoji copy, add the morning brief notification type, integrate notification preference offsets from notification-store, and register the SDK 55 notification handler in _layout.tsx.

Purpose: The existing notification-service.ts has clinical copy ("Time to Wind Down", "Last chance for caffeine") and no morning brief. Per D-06, D-07, D-08, NOTIF-04, notifications must be warm and emoji-rich. Per D-10, all notifications use expo-notifications local scheduling.

Output: Upgraded notification-service.ts + handler registration in _layout.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-night-sky-mode-notifications/04-01-SUMMARY.md

<interfaces>
<!-- notification-store (from Plan 01): -->
import { useNotificationStore } from '@/src/store/notification-store';
// useNotificationStore.getState() returns:
// { windDownEnabled, windDownLeadMinutes, caffeineCutoffEnabled, caffeineCutoffLeadMinutes, morningBriefEnabled }

<!-- existing notification-service.ts exports: -->
export async function requestPermissions(): Promise<boolean>
export async function scheduleSleepReminder(bedtime: Date, label: string): Promise<string | null>
export async function scheduleCaffeineCutoff(cutoffTime: Date): Promise<string | null>
export async function scheduleWakeReminder(wakeTime: Date): Promise<string | null>
export async function schedulePlanNotifications(planBlocks: PlanBlock[]): Promise<string[]>
export async function cancelAllNotifications(): Promise<void>
export async function getScheduledNotifications(): Promise<Notifications.NotificationRequest[]>

<!-- PlanBlock type: -->
// block.type: SleepBlockType ('main-sleep'|'nap'|'caffeine-cutoff'|'wind-down'|'wake'|'meal-window'|'light-seek'|'light-avoid')
// block.start: Date, block.end: Date, block.label: string

<!-- SDK 55 setNotificationHandler API (verified in installed package): -->
Notifications.setNotificationHandler({
  handleNotification: async () => ({
    shouldShowBanner: true,   // NOT shouldShowAlert (deprecated)
    shouldShowList: true,
    shouldPlaySound: true,
    shouldSetBadge: false,
  }),
});
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade notification copy and add morning brief function</name>
  <files>
    src/lib/notifications/notification-service.ts
    src/lib/notifications/index.ts
  </files>
  <behavior>
    - scheduleSleepReminder title: '🌙 Wind-down time' body includes 'sleep window opens in'
    - scheduleCaffeineCutoff title: '☕ Last call for caffeine' body: 'Caffeine cutoff in 30 minutes — switch to water or herbal tea'
    - scheduleWakeReminder title: '⏰ Time to rise' body: 'Your sleep window has ended — morning routine starts now'
    - scheduleWindDownNotification (internal) title: '🌙 Wind-down time' body references lead minutes from notification-store
    - scheduleMorningBrief(wakeTime, firstBlockLabel) → schedules at wakeTime with title '☀️ Good morning!' body: 'First up: {firstBlockLabel}'
    - scheduleMorningBrief returns null if wakeTime is in the past
    - schedulePlanNotifications reads prefs from useNotificationStore.getState() — skips scheduling winddown if windDownEnabled=false, skips caffeine if caffeineCutoffEnabled=false, skips morningBrief if morningBriefEnabled=false
    - schedulePlanNotifications calls scheduleMorningBrief for 'wake' blocks when morningBriefEnabled, using the label of the next block after the wake time as firstBlockLabel
    - Wind-down lead time uses windDownLeadMinutes from store (default 45 min), not hardcoded 30
  </behavior>
  <action>
Read src/lib/notifications/notification-service.ts first. Then rewrite keeping all existing function signatures intact (no breaking changes for plan-store.ts callers) while:

1. Update notification copy per D-06, D-07, D-08, NOTIF-04:
   - scheduleSleepReminder: title='🌙 Wind-down time', body uses windDownLeadMinutes from store
   - scheduleCaffeineCutoff: title='☕ Last call for caffeine', body='Caffeine cutoff in 30 minutes — switch to water or herbal tea'
   - scheduleWakeReminder: title='⏰ Time to rise', body='Your sleep window has ended — morning routine starts now'
   - scheduleWindDownNotification: title='🌙 Wind-down time', body=`Start winding down — bedtime in ${windDownLeadMinutes} minutes`

2. Add scheduleMorningBrief function:
```typescript
export async function scheduleMorningBrief(
  wakeTime: Date,
  firstBlockLabel: string,
): Promise<string | null> {
  if (wakeTime.getTime() <= Date.now()) return null;

  return Notifications.scheduleNotificationAsync({
    content: {
      title: '☀️ Good morning!',
      body: `First up: ${firstBlockLabel}`,
      data: { type: 'morning-brief' },
      sound: true,
    },
    trigger: {
      type: Notifications.SchedulableTriggerInputTypes.DATE,
      date: wakeTime,
    },
  });
}
```

3. Update schedulePlanNotifications to:
   a. Read `const prefs = useNotificationStore.getState()` at the top
   b. In the main-sleep/nap case: apply `prefs.windDownEnabled` check; use `prefs.windDownLeadMinutes` for lead time
   c. In the caffeine-cutoff case: check `prefs.caffeineCutoffEnabled`
   d. In the wake case: check `prefs.morningBriefEnabled`; find firstBlockLabel by searching planBlocks for the earliest block starting after block.start; call scheduleMorningBrief(block.start, firstBlockLabel ?? 'your schedule')

4. Update `src/lib/notifications/index.ts` to also export `scheduleMorningBrief`.

Write/update test in `__tests__/` or `src/lib/notifications/` — test schedulePlanNotifications with a mock plan that has wind-down, caffeine-cutoff, and wake blocks. Verify:
- scheduleNotificationAsync called with emoji titles
- When windDownEnabled=false, the wind-down notification is NOT scheduled
- scheduleMorningBrief is called for wake blocks when morningBriefEnabled=true
  </action>
  <verify>
    <automated>cd /Users/claud/Projects/ShiftWell && npx jest --testPathPattern="notification-service|notification" --no-coverage 2>&1 | tail -25</automated>
  </verify>
  <done>All notification service tests pass. grep '🌙' src/lib/notifications/notification-service.ts returns a match. grep 'scheduleMorningBrief' src/lib/notifications/index.ts returns a match.</done>
</task>

<task type="auto">
  <name>Task 2: Register SDK 55 notification handler in _layout.tsx</name>
  <files>
    app/_layout.tsx
  </files>
  <action>
Read app/_layout.tsx first. Then add the `setNotificationHandler` call per the SDK 55 breaking change (Pitfall 1 from research):

1. Add import at the top:
```typescript
import * as Notifications from 'expo-notifications';
```

2. Add a module-level call BEFORE the RootLayout component definition (not inside useEffect — handler must be registered before any notification fires):
```typescript
// Register foreground notification handler — SDK 55 API (shouldShowBanner replaces shouldShowAlert)
Notifications.setNotificationHandler({
  handleNotification: async () => ({
    shouldShowBanner: true,
    shouldShowList: true,
    shouldPlaySound: true,
    shouldSetBadge: false,
  }),
});
```

Place this directly after the existing imports, before `export const unstable_settings`. Do not add it inside a useEffect or component — it must run at module evaluation time.

Do not modify any other part of _layout.tsx.
  </action>
  <verify>
    <automated>cd /Users/claud/Projects/ShiftWell && grep -n "shouldShowBanner" app/_layout.tsx</automated>
  </verify>
  <done>`grep -n "shouldShowBanner" app/_layout.tsx` returns a line. `grep "shouldShowAlert" app/_layout.tsx` returns nothing (old deprecated API not used).</done>
</task>

</tasks>

<verification>
- `grep -c "🌙" src/lib/notifications/notification-service.ts` returns >= 2
- `grep -n "shouldShowBanner" app/_layout.tsx` returns a match
- `grep -n "useNotificationStore" src/lib/notifications/notification-service.ts` returns a match
- `grep -n "scheduleMorningBrief" src/lib/notifications/index.ts` returns a match
- `npx jest --no-coverage 2>&1 | grep -E "PASS|FAIL"` — no regressions
</verification>

<success_criteria>
- Notification service emits warm emoji copy for all three notification types (NOTIF-04)
- Morning brief function exists and is exported (NOTIF-03)
- schedulePlanNotifications respects enabled flags from notification-store (NOTIF-05 service layer)
- SDK 55 handler registered at app root — foreground notifications will display (NOTIF-01, NOTIF-02)
</success_criteria>

<output>
After completion, create `.planning/phases/04-night-sky-mode-notifications/04-02-SUMMARY.md`
</output>
