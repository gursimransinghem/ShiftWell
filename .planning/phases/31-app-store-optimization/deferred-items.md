# Deferred Items — Phase 31

## Pre-existing issues discovered but out of scope

1. `jest.config.js` has git merge conflict markers — tests cannot run. Pre-existing issue, not introduced by Phase 31 changes.
2. `src/lib/ai/weekly-brief-generator.ts` has git merge conflict markers (lines 308, 315, 338, 379, 419) — TypeScript compile errors. Pre-existing, not introduced by Phase 31.
