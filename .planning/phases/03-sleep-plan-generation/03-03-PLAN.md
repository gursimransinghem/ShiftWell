---
phase: 03-sleep-plan-generation
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified: []
autonomous: false
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-03
  - PLAN-04
  - PLAN-05
  - PLAN-06

must_haves:
  truths:
    - "Today screen shows a meaningful WHAT'S AHEAD message when shifts are present"
    - "Calendar contains ShiftWell events for all plan block types after plan generation"
    - "Removing a shift from the calendar causes plan to regenerate and calendar events to update"
    - "Free morning days (off/recovery with no early personal events) show no alarm block"
    - "commuteDuration from onboarding is reflected in wake-up times"
  artifacts:
    - path: "src/components/today/SchedulePreview.tsx"
      provides: "SchedulePreview component"
    - path: "src/store/plan-store.ts"
      provides: "Plan store with calendar write-back"
    - path: "src/lib/calendar/plan-write-service.ts"
      provides: "Calendar write service"
  key_links:
    - from: "app/(tabs)/index.tsx"
      to: "src/components/today/SchedulePreview.tsx"
      via: "renders WHAT'S AHEAD section"
      pattern: "WHAT'S AHEAD"
---

<objective>
Visual and functional verification of Phase 3 — confirm all PLAN requirements are working end-to-end in the running app.

Purpose: Phase 3 is wiring work. Automated tests verify the logic. This checkpoint confirms the user-facing behavior: plan generation fires on calendar changes, calendar events appear for all block types, Today screen shows preview messaging, and Circadian Reset works.

Output: User confirmation that Phase 3 success criteria are met.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-sleep-plan-generation/03-CONTEXT.md
@.planning/phases/03-sleep-plan-generation/03-01-SUMMARY.md
@.planning/phases/03-sleep-plan-generation/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final test suite + build validation</name>
  <read_first>
    - .planning/phases/03-sleep-plan-generation/03-01-SUMMARY.md
    - .planning/phases/03-sleep-plan-generation/03-02-SUMMARY.md
  </read_first>
  <files></files>
  <action>
    Run the full automated validation suite. All must pass before the checkpoint.

    1. Full test suite: `npx jest --passWithNoTests`
       - Expected: 237 baseline + new plan-store tests = all green
       - If failures exist: diagnose and fix before proceeding

    2. TypeScript compile: `npx tsc --noEmit`
       - Expected: zero errors
       - If errors exist: fix type issues before proceeding

    3. Verify plan-store wiring:
       ```bash
       grep -n "writeChangedBlocks\|recalculationNeeded\|lastResetAt\|debouncedRegenerate" src/store/plan-store.ts
       ```
       Expected: All four patterns present.

    4. Verify SchedulePreview in Today screen:
       ```bash
       grep -n "SchedulePreview\|WHAT'S AHEAD\|lastResetAt" app/(tabs)/index.tsx
       ```
       Expected: All three patterns present.

    5. Verify plan-write-service exports:
       ```bash
       grep -n "^export" src/lib/calendar/plan-write-service.ts
       ```
       Expected: writePlanBlock, writeChangedBlocks, blockChanged, buildPlanBlockTitle all exported.

    6. Check no algorithm files were modified (IP protection):
       ```bash
       git diff --name-only HEAD | grep "src/lib/circadian/"
       ```
       Expected: empty output (no circadian module changes).

    If any check fails, fix it before proceeding to the visual checkpoint.
  </action>
  <verify>
    <automated>npx jest --passWithNoTests --silent && npx tsc --noEmit && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>All automated checks pass. "ALL CHECKS PASSED" appears in output. Zero TypeScript errors. Zero test failures.</done>
  <acceptance_criteria>
    - `npx jest --passWithNoTests --silent` → exits 0
    - `npx tsc --noEmit` → exits 0, zero output
    - writeChangedBlocks present in plan-store.ts
    - SchedulePreview present in app/(tabs)/index.tsx
    - No changes to src/lib/circadian/* files
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — Today screen and calendar write-back</name>
  <files></files>
  <action>Run the app and verify Phase 3 end-to-end: plan generation, calendar write-back, SchedulePreview, Circadian Reset. See how-to-verify for exact steps.</action>
  <verify>User confirms "approved" after completing all 5 visual checks described in how-to-verify.</verify>
  <done>User types "approved" after verifying all 5 checks pass in the running app.</done>
  <what-built>
    Complete Phase 3 sleep plan generation pipeline:
    - Plan store now writes all plan block types (sleep, nap, caffeine cutoff, meal window, light protocols) to ShiftWell Apple calendar after generation
    - Circadian Reset: when shifts are removed from calendar, plan regenerates and optimizes for circadian recovery
    - Today screen shows "WHAT'S AHEAD" section with forward-looking preview messages
    - Debounce prevents redundant regenerations on rapid calendar changes
  </what-built>
  <how-to-verify>
    Run the app: `npx expo start` or `npx expo run:ios`

    **Check 1 — Plan generation (PLAN-01, PLAN-02):**
    1. Open the app on a device/simulator with Apple Calendar connected
    2. Add a night shift event to your calendar for next week
    3. Wait for background sync (or pull to refresh on Today screen)
    4. Open Apple Calendar app → look for "ShiftWell" calendar
    5. Verify new events appear: "Sleep — 11:00 PM", "Nap — 2:30 PM", "Caffeine Cutoff — 2:00 PM", "Meal Window — ...", light protocol events
    6. Expected: All block types present in ShiftWell calendar, not just sleep blocks

    **Check 2 — Commute-aware wake time (PLAN-03):**
    1. In the app, check your commute duration in Settings/Profile (should be what you entered in onboarding)
    2. Find a day shift in the plan
    3. Verify: Wake time = shift start - commute duration - AM routine total
    4. Example: 7am shift, 30min commute, 45min AM routine → wake at 5:45am

    **Check 3 — Free morning detection (PLAN-04):**
    1. Find an off day in the plan (no shifts scheduled)
    2. On Today screen for that day — verify no alarm block appears
    3. If you have personal events before 10am on that day, a wake time SHOULD still appear
    4. Expected: Pure off days with no early personal events = no alarm

    **Check 4 — Schedule preview (PLAN-05):**
    1. With night shifts in the next 2 weeks, Today screen should show "WHAT'S AHEAD" section
    2. Expected message examples:
       - "3 nights ahead — pre-adapt starting Thursday"
       - "2 night shifts in the next 2 weeks"
       - "Free days ahead — sleep in opportunity" (if no upcoming shifts)
    3. Verify the section does NOT appear when there are no shifts at all

    **Check 5 — Circadian Reset (PLAN-06):**
    1. Remove a night shift from your calendar
    2. Wait for sync or pull to refresh
    3. Today screen should show "Circadian Reset — returning to day rhythm" in warm gold
    4. Open ShiftWell calendar — sleep block events for the removed shift's dates should be deleted
    5. New rest-optimized sleep blocks should appear for the freed-up dates

    **If anything looks off:** Describe what you see vs. expected. Type "issues: [description]"
    **If everything checks out:** Type "approved"
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 3, or "issues: [description]" to report problems</resume-signal>
</task>

</tasks>

<verification>
Phase 3 complete when:
- [ ] All automated tests pass (237 + plan-store tests)
- [ ] TypeScript compiles clean
- [ ] User confirms visual verification passes
- [ ] PLAN-01 through PLAN-06 all addressed
</verification>

<success_criteria>
Phase 3 success criteria from ROADMAP.md:
1. After connecting calendar, app generates a full sleep plan (sleep windows, naps, caffeine cutoffs, meal timing, light protocols) — verified by Check 1
2. Wake-up times account for commute — 30-minute commute shifts alarm 30 minutes earlier — verified by Check 2
3. Free mornings detected and plan proposes sleeping in — verified by Check 3
4. Today screen shows "preview" message like "3 nights next week — pre-adapt starting Thursday" — verified by Check 4
5. When calendar changes, sleep plan recalculates and calendar updates — verified by Check 5
</success_criteria>

<output>
After completion, create `.planning/phases/03-sleep-plan-generation/03-03-SUMMARY.md`
</output>
